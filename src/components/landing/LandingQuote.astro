<section id="studio" class="lp-quote">
  <div class="container">
    <blockquote>Não fabricamos software. Construímos produtos digitais.</blockquote>
    <p class="attrib"><strong>Code Studio 92</strong> · voz do studio</p>
  </div>
</section>

<style>
  .lp-quote {
    padding: var(--space-4xl) 0;
    border-bottom: 1px solid var(--border);
    text-align: center;
  }

  .container {
    max-width: 720px;
    margin: 0 auto;
    padding: 0 var(--space-xl);
    width: 100%;
  }

  .lp-quote blockquote {
    margin: 0 0 var(--space-lg);
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(24px, 3.5vw, 36px);
    line-height: 1.3;
    letter-spacing: -0.015em;
    color: var(--text1);
  }

  .attrib {
    margin: 0;
    font-family: var(--font-body);
    font-size: var(--text-body-sm);
    color: var(--text3);
  }

  .attrib strong {
    color: var(--text2);
    font-weight: 500;
  }
</style>
